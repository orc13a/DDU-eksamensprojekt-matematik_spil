class Component {
    constructor() {
        this.x;
        this.y;
        this.w; // Width
        this.h; // Height
        this.img; // Image
        this.pos = createVector(0, 0); // Position
        this.vel = createVector(0, 0); // Velocity
        this.loc = createVector(0, 0); // Location
        this.spd = createVector(0, 0); // Speed
    }
}